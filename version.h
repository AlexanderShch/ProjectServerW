#pragma once

// Строка версии для отображения
static const wchar_t* const VERSION_STR = L"2.1.3";

/*
Версия 2.1.2
- было ложное срабатывание по тайм-ауту _Wrk=1, а не останов по температуре
Добавил защиту от ложного останова: теперь нужен не один, а два подряд тика таймера с превышением порога.
При любом валидном _Wrk=1 счётчик сразу сбрасывается.

Версия 2.1.1
- Реализована единая очередь отправки и ожидания ответа на команды

Версия 2.1.0
- Добавлена команда REQ_CMD_GET_ALARM_FLAGS для получения регистров аварий.
- Добавлен параметр debugDisableTargetTStop для отключения автостопа по целевой температуре.
- Добавлен параметр debugDisableDeviceSwitchCheck для отключения проверки соответствия входов/выходов.
- добавлена вкладка "АВАРИЯ" для отображения аварийных устройств и датчиков.
- В файл с данными в Excel добавлена вкладка "АВАРИЯ" для аварийных устройств и датчиков.

Версия 2.0.2
- Автоматическое чтение параметров с контроллера выполняется только при соединении с устройством
- При соединении с устройством выполняется чтение параметров с контроллера и отображение в таблицах на вкладке "Параметры"

Версия 2.0.1
- Уменьшен таймаут ожидания ответа на команду RESET до 100 мс
- Перенесены стартовые команды на переподключение в отложенную инициализацию
- Добавлены логи о переподключении контроллера

Версия 2.0.0
- Переписана логика обмена данными с контроллером
 
Версия 1.6.3
- Добавлены колонки T_filt_0…T_filt_6 и phase в таблицу 2 на вкладке «Данные» для отображения отфильтрованных температур датчиков и фазы работы алгоритма.

Версия 1.6.2
- Добавлен параметр fishColdTarget_C (целевая минимальная температура рыбы) в группу 6 (LOG_GLOBAL): чтение/запись с контроллером и отображение в таблице 2 на вкладке «Параметры».
- Добавлена информация в лог о достижении целевой температуры рыбы.

Версия 1.6.1
Изменена логика обмена между сервером и контроллером. Теперь мастером на шине является сервер.
Только мастер может отправлять команды на контроллер, после чего контроллер может ответить серверу, а сервер будет ожидать ответа.

Версия 1.5.12
- Таймаут отсутствия регулярного лога для сброса флага «авторежим» = интервал измерений (с вкладки «Настройки») + 1 с.
- Загрузка параметров из Excel: кнопка «Загрузить», выбор файла WorkData_*.xlsx, листы «Параметры по фазам» и «Параметры общие» загружаются в таблицы 1 и 2.
- Запись параметров в контроллер группой (SET_DEFROST_GROUP); при сохранении в Excel существующие листы параметров перезаписываются.
- Исправлен вызов Workbooks.Open в C++/CLI (15 параметров); макросы заменены на constexpr; уменьшена ширина колонки указателей строк в таблицах параметров.

Версия 1.5.11
- По кнопке «Чтение параметров» запрашиваются группы 5 и 6 (REQ_CMD_GET_DEFROST_GROUP):
  данные принимаются как бинарные структуры, аналогичные контроллеру, и отображаются в двух таблицах
  (фазозависимые параметры и общие параметры).
- Увеличен MAX_COMMAND_SIZE для приёма расширенных ответов, поправлена визуализация таблиц параметров
  (высота строк, ширина столбцов).

Версия 1.5.10
- Команда RESET: при таймауте ответа не планируется зонд GET_CMD_INFO, вместо этого вызывается SchedulePostResetInit (ожидание переподключения контроллера).
- При таймауте RESET в интерфейсе выводится «Сброс отправлен, ожидание переподключения...» и запускается отложенная инициализация (GET_VERSION, SET_INTERVAL после переподключения).
- При отбрасывании «чужого» ответа в ожидании ответа на RESET зонд GET_CMD_INFO не планируется.

Версия 1.5.9
- Файл с данными в EXCEL записывается по команде СТОП
- Запоздалые ответы GET_VERSION и SET_INTERVAL применяются через ApplyLateStartupResponse.
- После переподключения (и при повторе после RESET) добавлена пауза 1,5 с перед первой командой — для стабилизации моста TCP↔UART.

Версия 1.5.8
Команда GET_CONFIG удалена; состояние ПУСК/СТОП выставляется по приходу лога параметров.

Версия 1.5.7
Полудуплекс: пауза в потоке приёма после освобождения recvGate увеличена с 50 до 300 мс.

Версия 1.5.6
При переподключении вместе с GET_VERSION отправлялся GET_CONFIG; в 1.5.8 заменено на реакцию по логу параметров.

Версия 1.5.5
- Таймаут ожидания ответа для GET_VERSION и GET_CMD_INFO увеличен до 6 с (вместо 2 с).
- Пакеты Type=0x01 с Len!=65 обрабатываются как ответ на команду PROG_CONTROL (START/STOP), а не отбрасываются как «не тот лог».

Версия 1.5.4
Добавлен функционал кнопок СТАРТ и СТОП для запуска и остановки дефростера в автоматическом режиме

Версия 1.5.3
Все параметры процесса дефростации разделены на группы.
Группа 3 - переменные процесса, передаются на сервер только только в автоматическом режиме работы и
Записываются в таблицу вместе с телеметрией.

Версия 1.5.2 подчистка русской литерации

Версия 1.5.1 (Mar 02 2026)
- добавлены параметры работы без датчиков Т продукта
- дополнительные параметры добавлены в обмен с сервером

Версия 1.5.0 (Mar 02 2026)
- добавлен API для обмена данными параметров с контроллером
- добавлена вкладка Параметры, где отображаются параметры алгоритма дефростации
- Параметры можно изменять вручную и сохранять в файл параметров

1.4.2 добавлена команда установки интервала передачи данных

1.4.1 откорректированы названия столбцов и цвета графиков в Excel

1.4.0 
1. изменена очередь команд. Теперь все команды для одного сокета отправляются в очередь 
и обрабатываются в отдельном потоке
2. изменена структура кадра данных, поступающих от контроллера. Новый формат без маркера конца и содержит длину кадра: 
AA 55 + Type + Len + Data[Len] + CRC16, где CRC считается по Type+Len+Data. 

1.3.22 Полудуплекс: в потоке приёма после освобождения recvGate добавлена пауза 50 мс, чтобы поток UI успевал захватить замок и отправить стартовые команды (устранена задержка 20+ с при запуске сервера).

1.3.21 Полудуплекс: после захвата recvGate перед send() добавлена задержка 150 мс (время разворота линии), чтобы команды при переподключении не терялись из-за отправки сразу после приёма телеметрии.

1.3.20 Лог: все русские сообщения возвращены в виде обычного текста в UTF-8 (убраны Unicode-экранирования); вызовы LogMessage(ConvertToStdString(...)) заменены на LogMessage(String^) для корректной записи в log.txt в UTF-8.

1.3.19 Лог: log.txt пишется в кодировке UTF-8 (StreamWriter с Encoding::UTF8). Русский текст отображается корректно при открытии в UTF-8.

1.3.18 Лог: все сообщения с русским текстом переведены на LogMessage(String^) или Unicode-литералы, кракозябры в log.txt устранены.

1.3.17 Полудуплекс: recvGate берётся в начале каждой итерации цикла (до recv()) и освобождается в finally после обработки — отправка команд блокируется на всё время приёма и ожидания в recv(). Сообщение «Получены новые данные» в лог через Unicode-литерал (без кракозябр).

1.3.16 Лог: перегрузка LogMessage(const char*) трактует узкие литералы как UTF-8; сообщения из String^ и исключения пишутся через LogMessage(String^). Файл log.txt в CP1251. Исходники с русским текстом — в UTF-8.

1.3.15 Удалена обработка бита Work: синхронизация кнопок, таймер финализации и экспорт по Work=0 больше не используются. При первом приёме данных — отправка GET_VERSION/SET_INTERVAL и показ START. Лог исключений пишется через LogMessage(String^) без кракозябр.

1.3.14 (устарело) Логику финализации данных поменял — в 1.3.15 логика по биту Work удалена.

1.3.13 Откорректирована логика определения окончания работы программы дефростера
1. таймер "нахождения Work в нуле" и OnDelayedExcelTimerTick удалены,
2. финализация/экспорт и запуск START в сценарии автоперезапуска выполняются только при получении телеметрии, когда выполнено Work=0 и Open=1.

1.3.12 увеличен таймаут ожидания остановки для автоперезапуска

1.3.11 исправлен таймаут ожидания остановки для автоперезапуска

1.3.10 
1. добавлена команда GET_CMD_INFO для получения информации о последней команде
2. исправлена причина создания двух окон для приёма данных
3. если ответ на команду не получен или истек таймаут ожидания ответа, выполняется запрос GET_CMD_INFO

1.3.9 добавлено логирование начала записи данных после повторного подключения

1.3.8 добавлен перезапуск по времени

1.3.7 исправлены ошибки при закрытии формы
 
1.3.6 создана очередь для экспорта данных в Excel:
все отдельные потоки и разные события одного потока помещают данные для сохранения в общую очередь 
для одного STA-потока экспорта данных в Excel
*/
